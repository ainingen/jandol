# 保留リスト

いま手をつけないと決めたもの。着手するときは、それぞれの「動かす前に」を先に読むこと。

---

## いま開いているもの（2026年9月7日）

**対局画面の作り直しは `main` に入った。**段1〜段8・効果音13本・局の締めの帯と点棒・
実機で出た指摘まで全部（`docs/design/match/spec.md` と `agari-spec.md`）。
**実機で確かめた決めごとは、動かさないこと**——河の読みやすさ・卓の横長 1:2.2・
`--th` の 0.7 / 0.581（`match.css` のコメント）・**「おまかせ」の釦は右上**
（`match.css` の `body.inMatch.four #topbar`。誤って押すと局が飛ぶ釦なので、
多少遠いほうが安全側。2026年9月6日に実機で確定）。

### 公開の状態（ここを取り違えると判断がずれる）

**GitHub Pages は開発中の確認用。遊んでいる人はいない。**
公開しているのは PLiCy で、そちらは**手で差し替えるまで古いまま。**
**`main` を更新しても公開中のゲームは変わらない。**

PLiCy への公開はまだ先（作り込みを続けるため）。
公開する日の手順は `README.md` の「公開」の項にある。

### 実機で確認するときの手順

Windows の `C:\dev\jandol` で `git pull` → `python -m http.server 8000`。
iPhone から `http://<PCのIP>:8000/match.html?v=<毎回違う数字>`。

- **番号を毎回変えること。**変えないと古いコードが動き続ける
- **マナーモードを切ること。**入っていると WebAudio が黙る
- **Safari でも Chrome でも、四人卓は対局中に再読み込みされる**（下の項）。
  **ブラウザを選んでも避けられない。**いまは復帰の仕組みで凌いでいる

理由と、そこで踏んだ罠は `HANDOVER.md` §5 の三つ
（「iOS Safari は JavaScript を強く握る」「PC で見えるものと実機で見えるものは違う」
「iOS のマナーモードが WebAudio を黙らせる」）。**ここには手順だけ置く**
——二重に書くと片方が古くなる。

### 四人卓のときだけ、対局中に再読み込みされる（原因は未特定）

**iPhone の実機で、四人卓（`body.inMatch.four`）のときだけ**、対局中にページが
再読み込みされる。**Safari も Chrome も落ちる。**落ちるまでの打牌数はばらつく。
**縦持ちの列レイアウトでは落ちない。縦持ちのまま「回転」で四人卓にすると落ちる**ので、
**端末の向きは無関係で、`body.inMatch.four` が持ち込んでいる何か**が容疑。
描画プロセスが落ちて、ブラウザがページを読み直している、という読み。

**原因は未特定。**以下は潰した（2026年9月7日）。

| 試したこと | 道具 | 結果 |
|---|---|---|
| 3D（`perspective` / `rotateX` / `preserve-3d` / 牌の厚み）を全部外す | `?flat=3` | **落ちた** |
| `fitFour()` の 500ms 常時実行を止める | `?nofit=1` | **落ちた** |
| CPU だけで打たせる（指を触れない） | `?auto=1` | **落ちた** |

**削っても落ちるまでの時間が伸びない。**「どこか一箇所の重い処理」を探す方針では
当たらないと判断した。

**落ちても続けられるようにはした**（2026年9月7日。下の「局の頭からの復帰」）。
**これは直したものではない。**原因の追跡はやめない。
大会の設定画面に**縦持ちを勧める一行**を出してある（`taikai.js` の `tkSetNote`）
——マナーモードの注記と同じ書き方で、**警告にはしていない。**
起きていない人を不安にさせないため。

**以前ここには「iOS Chrome だけ・Safari では落ちない」と書いてあった。**
2026年9月6日の時点でそう見えていたが、**Safari でも落ちる。**
「Chrome × タブ多数の条件かどうか」を切り分ける、という次の一手も**要らなくなった**
——ブラウザの問題ではない。**古い読みに戻らないこと。**

#### いまは復帰の仕組みで凌いでいる（2026年9月7日）

**落ちる理由に依存しない対処を先に入れた。**落ちてもいいから、**局の頭から**
続きを打てるようにしてある。仕様と段の分けかたは
**`docs/design/match/resume-spec.md`**。

- 局の頭（`deal()` の直後）で `kyoku` / `honba` / 供託 / 4人の持ち点 / 起家 /
  席のキャラIDを `localStorage` に控える
- 読み直したとき、控えがあれば `match.html` が一言と釦二つを出す
- **その局の手は消える。配牌は引き直し**——だから黙ってやらず、必ず断る

**これは応急処置で、落ちること自体は残る。**原因の追跡はやめない。
**いまは `match.html` だけで効く**（本編に入れるかは下の「次にやる候補」）。

#### 切り分けの道具（`match.html`。**消さないこと**）

    ?flat=1   牌の厚み（::before）を消す
    ?flat=2   ＋ 牌そのものの3Dをやめる
    ?flat=3   ＋ 卓の傾きと遠近をやめる
    ?nofit=1  卓の寸法を測り直す 500ms の見張りを止める（寸法は初回で固定）

**原因追跡用に残してある。**上の三つを潰しただけで終わりではないので、
**原因が分かるまで消さない**（消すときはその理由をここに書く）。

`?nofit=1` は `match.js` の `setInterval(…, 500)` を止めるもの。`Match` は
`fitFour` を外に出していないので、`src/` を読む前に `window.setInterval` を
差し替えて**間隔 500 の登録だけ握り潰す**。**寸法は初回の一回だけで固定され、
以後は追随しない**（画面の向きを変えても直らない）。`?flat=` と一緒に使える。

右下に `flat=N` / `nofit` の印が出る。**印が無ければ効いていない**
——実機では開発者ツールが使えないので、「効かなかった」と「効いたけど落ちた」を
これで分ける。絵は崩れてよい。**打てて落ちないかだけを見る。**

**`src/match.css` には書かないこと。**GitHub Pages が `src/*.css` を10分キャッシュするので、
実機で古いものを見たまま「効かない」と誤読する。**`match.html` はURLにクエリが付くので
必ず新しく取れる**——だからインラインの `<style>` と `<script>` だけで完結させてある。

**動かす前に：**`HANDOVER.md` §5「締めの帯の下に、押せるものを置かない」を読むこと。
この症状を追っているときに帯の下の重なりを直したが、**それはこの件の原因ではない。**
同じところをもう一度疑わないため。

### 事務所を横持ちで開くと、遠征先の当たり判定がタブ帯に隠れる

遠征中の朝、店の客を叩いたつもりが**下のタブ帯の「チーム」に飛ぶ**（844×334 で確認）。
客の当たり判定（`.jnFlHit`）が画面の下端まで伸びていて、その上にタブ帯が乗っているため。
**380×740（縦）では起きない。**

**横持ちは対局中だけの姿勢**なので優先度は低い。ただし **iOS は勝手に回る**ので、
遠征中に横に倒したまま店を触る人はいずれ出る。**この項目を消さないこと。**

**動かす前に：**事務所の画面ぜんたいが縦の寸法で設計されている（`office/room.md` は 380px）。
当たり判定だけ縮めても、そのとき見えている絵は縦向けのままになる。
**「横持ちの事務所をどう見せるか」を決めてから触ること。**
`node tools/drive-office.js --real` は縦（380×740）で回す前提で、
横で回すとこの当たりに掛かる（あの道具の冒頭に書いてある）。

### `finish()` が `comp` を持たない契約済みの子で落ちる（気になる脆さ）

`taikai.js` の `finish()` の育成で `before.toFixed(1)` を呼ぶが、
`before` は `st.comp[id] ?? c.comp` で、**`JANDOLS` は `comp` を持たない**
（持っているのは `FREE_AGENTS` だけ。13〜21）。`st.comp` にその子の完成度が
入っていないと `undefined.toFixed()` で落ちる。

**本編の経路では起きない。**契約する口は二つ（`scout.js` の「契約する」と
`office.js` の遠征の交渉）で、どちらも `comp[c.id]` を `compFromRank` で埋めてから
`contracted` に足している。**踏むのはセーブの形が崩れたとき**——
`contracted` にいるのに `comp` が空、という組み合わせ。
`debug.html` のプリセットや、手で書いたセーブで起きる（段3 の検証で実際に踏んだ）。

**直すなら `finish()` ではなく入口を疑うこと。**あそこで `?? compFromRank(rank)` に
落とすと、**壊れたセーブが黙って動いてしまい、どこで崩れたのか分からなくなる。**
落ちること自体は正しい振る舞いかもしれない——**決めてから触ること。**

### 元データの置き場所

`audio_raw/` は `.gitignore` に入れてあり、**リポジトリには無い。**
生成音を作り直すときは、`audio_raw/sfx/` の8本と `audio_raw/discard/` の4本を
置き直してから `python3 tools/prep-sfx.py`。切り出し位置と音量の目標は
あの道具の `SOURCES` に一行ずつ書いてある。

---

## 次にやる候補と、その前に決めること（2026年9月7日）

**優先度はまだ決めていない。**ここにあるのは材料だけ。
各項目の「先に決めること」が片づかないうちは着手しないこと
——**決めずに始めると、作ったものを捨てることになる。**

| 候補 | 先に決めること |
|---|---|
| **掛け声と和了ゼリフの声** | **声が19種そろうか**（下に別枠で書いた）。**ここが最初の関門** |
| **ローカルルール** | **卓を3〜4種に束ねる中身**。個別のトグルにしないこと |
| **横持ちの事務所** | **「横持ちの事務所をどう見せるか」。**当たり判定だけ直しても、絵は縦向けのまま |
| **`renderGrid()` の最適化** | **無い。**ただし実測147msで、まだ体感で困っていない。**遅くなってから** |

### 局の頭からの復帰 → **済んだ。ただし大会だけ**（2026年9月7日）

`docs/design/match/resume-spec.md`（対局の側）と
`docs/design/taikai/resume-spec.md`（大会の側）。**段4（実機）まで通った。**

- `src/resume.js` は `build.py` の JS に入り、`index.html` でも効く
- `Match.play` は控えを消さず `done` を書く。**消すのは呼び出し元**
  （`match.html` は `await` のあと、大会は `finish()` のあと）
- `st.pendingTaikai` に大会の進行を控える。表紙の「続きから」で出走表へ戻り、
  **打った回戦は打ち直さない**

**実機で確かめたこと**（一回の走行で三つとも踏んだ）：

- **途中の回戦から。**大会の最中に落ちて、表紙 → 続きから → 出走表に一言 →
  ［続ける］→ **打ち終わった回戦は打ち直されず**、その先から進んだ
- **結果画面で落ちて `done` から。**順位を眺めているあいだに落ちた回。
  控えは `done` 付きで残っていて、**その卓は打たずに結果として使われた**
  ——`Match.play` が消すのをやめて呼び出し元へ移した効き目がここに出る
- **対局の途中の局から。**打っている最中に落ちた回。局・本場・持ち点・親が
  控えどおりで、そこから続けられた

**落ちずに終わった大会のあと**、開き直して一言が出ないことも確認済み。

**復帰したのは大会だけ。**`Match.play` へ入る道は5本あるが、
落ちたときに失うものが道ごとに違う（`taikai/resume-spec.md` §0）。

| 経路 | 落ちると | いまの扱い |
|---|---|---|
| 1 大会 | 賞金・成長・段位・戦績を失い、依頼も消える | **復帰する** |
| 2・3 雀荘（ボトル勝負／代表の夜の卓） | その日を打ち直し | やり直し |
| 4 遠征の交渉 | その日の交渉が消える（日は正しく進む） | やり直し |
| 5 遠征先の誘い | 声かけ回数が戻る | やり直し |

**経路 2〜5 は「やり直し」のままにする。**雀荘は打ち直せばよく、遠征は翌日もう一度できる
——**やり直せないのは大会だけ**だったので、そこに絞った。
**必要になったら足す**（`taikai/resume-spec.md` §10）。雀荘は `playDay` の途中、
遠征は `runTripDay` の中なので、いずれも「呼び出し元」を控える設計がそれぞれ要る。

**まだ確かめていないこと：PLiCy の iframe で `localStorage` が使えるか。**
使えなければ復帰も動かない（本編のセーブそのものが同じ前提の上に乗っているので、
使えないなら復帰だけの話では済まない）。

**落ちること自体は残っている。**復帰はそれを直したものではなく、
**落ちても続けられるようにしただけ**（原因は未特定。上の「いま開いているもの」）。
大会の設定画面に**縦持ちを勧める一行**を出してある（`tkSetNote`）
——マナーモードの注記と同じ書き方で、**警告にはしていない。**

**動かす前に：**`match/resume-spec.md` §10 と `taikai/resume-spec.md` §8
「触らないもの」を読むこと。**`UI.update` に保存を乗せないこと**
——あれは局中に何十回も呼ばれる。

### 掛け声と和了ゼリフの声 — **着手の前に数えること**

**ElevenLabs のライブラリに、日本語の女性の声が「聞き分けられる形で」19種そろうか。**
`serifu.js` の `LINES` は `chara`（性格）19種で、声をそこに紐づける設計になる。
場面を6つに絞っても **19 × 6 = 114本**。

**そろわなければ、`chara` 19種に紐づける設計そのものが成立しない。**
114本を作ってから気づくと手遅れになる。**数えるのが先。**

数えるときに一緒に決めること。

- **19種そろわなかったら、どうするか。**性格を束ねて声を減らす（例：8種）／
  声の数だけ性格に紐づけて残りは無声／声はやめる、のどれか。
  **「近い声を使い回す」は選ばないこと**——聞き分けられないなら、
  性格に紐づけている意味が無い
- **どの6場面か。**`LINES` の場面は11種ある
  （`start riichi tsumo ron deal call draw idle scoutMeet scoutWin scoutLose`）。
  **`idle`（雑談）は数が多いので声には向かない**——鳴る回数が桁で違う
- **音の作法は効果音と同じ**（`tools/prep-sfx.py` の `SOURCES`、
  A特性RMSで音量をそろえる、`audio/LICENSE.txt` の出典欄）。
  **`make-sfx.py` に書かせないこと**

**動かす前に：**`ui.js` の `sfx()` は論理名で鳴らしていて、
`NAMES` は9つのまま（`tools/drive-match.js` がそれを数えている）。
声を足すなら**そこを増やすのか、別の口を作るのか**を先に決める。

## 公開前に直したいもの

### `fitFour()` が 0.5秒ごとに卓を測り直している（重い）

`Match.play` が `setInterval(…, 500)` で `fitTable()` を回し続け、その中の
`fitFour()` が**二分探索で `--side` を入れては外接矩形を測る。**実測で
**`getBoundingClientRect()` を1回あたり166回・195〜275ms。**それが**0.5秒ごと**。

**これは再読み込みの原因ではなかった**（`?nofit=1` で止めても落ちた。上の項の表）。
**原因ではないと分かったので、ここへ移した**——性能の話として別に片づける。

**それでも重い。**四人卓のあいだ、半分の時間を測り直しに使っている計算になる。
電池も食うし、遅い端末では打牌の手応えに出る。

**動かす前に：**

- **数値を決め打ちにしないこと。**36度倒したうえに透視が掛かっているので、
  `cos(36°)` の計算値と実際の外接矩形は合わない。**測って決めているのには理由がある**
  （`match.js` の `fitFour` のコメント）
- **見張りを消すだけにしないこと。**局が変わると河が空になって卓の中身が縮むので、
  そのとき詰め直す必要がある。**「いつ測り直す必要があるか」を先に数えること**
  ——向きが変わったとき・`visualViewport` が動いたとき・局が変わったとき、で足りるはず。
  0.5秒ごとに測っているのは、**そのどれでもない取りこぼしを拾うための保険**
- `?nofit=1` はこの見張りを止める道具として使える（**寸法が初回で固定される**ので、
  向きを変えると崩れる。診断用であって直しかたではない）

### 白（`tiles/Haku.svg`）が真っ白 → **直った（2026年9月7日）**

配布元の `Haku.svg` は Inkscape の残骸（`clipPath`・`marker`・ガイド線）だけで、
**描画される要素がゼロ**だった。枠を一つ描いて差し替えた。

**採った値：色 `#19328c`（南・西の実物の青。東・北は `#142896`）・太さ20・
角丸10・内側36px。**`viewBox` は他の牌と同じ 300×400。

**細い（太さ10）・淡い（`#7c93d4`）・二重（外＋内、各10）は、河の大きさ（15〜21px）に
並べて落とした。**細いのは枠が消えて無地の板に戻り、淡いのは象牙の面に沈み、
二重は二本が潰れて一本の太い枠にしか見えない——**どれも「白だと分かる」まで届かない。**
七つの字牌を並べて、白だけ浮いていないことも確認済み。

`tiles/LICENSE.txt` の「取得したまま無改変」も書き換えてある
——**一本でも描き足したらあの一行は嘘になる。**

## 対局画面で外したもの（`docs/design/match/spec.md` §9）

8段はすべて入った。四つ外したうち、**和了画面と点棒は入った**
（`docs/design/match/agari-spec.md`・2026年9月5日）。残りは二つ。

### `render()` 全体の keyed 化

keyed なのは手牌と四つの河だけ。席プレート・コンパス・吹き出しは innerHTML で組み直している。
いまのところ体感で困っていない。**動かす前に：**牌の `Map<id,node>` の作法（`spec.md` §3.3）に揃える。
和了画面（`agari-spec.md`）を入れたあとも困っていないので、そのまま置いてある。

### ボイス

**200人ぶんは現実的でない。**いまは効果音だけ。
**鳴る12本は ElevenLabs の生成音に差し替え済み**（2026年9月4日）。
差し替えは `audio_raw/` に置いて `tools/prep-sfx.py` を回すだけ（`SOURCES` に行を足す）。
`audio/LICENSE.txt` の出典欄も一緒に直すこと。

**キャラ単位ではなく `chara`（性格）19種に紐づけるなら、話は変わる**
——下の「次にやる候補」の「掛け声と和了ゼリフの声」を読むこと。
**着手の前に確かめることがある。**

---

## 他の作業を待っているもの

### ローカルルール（卓ごとのルール差）

雀荘や大会によってルールが違う、という仕組み。喰いタンあり／なし、割れ目、赤の枚数、
一発裏など。臨場感のために入れたい。

**着手前提：**対局画面の改装が `main` に入っていること。

**先にやる小さいこと**（これだけ切り出して先行してよい。**2026年9月5日に入れた**）

`Game` のコンストラクタに `opts.rules` を受け取る口を開け、既定値 `RULES_DEFAULT` を
一つ置く。**`undefined` なら既定に落ちること。**分岐は一つも実装しなくてよい。

理由が二つ。`opts` を受け取る形にしたばかりなので、口を開けるのはいまが一番安い。
**後回しにすると、実装時に「ルールをどこから読むか」を全ファイルで探すことになる。**
そして雀荘や大会にルールを持たせると `state` にルール設定が乗るので、
**前方互換の形を最初に作っておかないと既存のセーブを壊す。**

**設計方針**（実装時に詰める）

**個別のトグルにしないこと。**喰いタン・後付け・割れ目・赤の枚数・一発裏・ハコ下・
順位点を全部トグルにすると数百通りになり、**AI の押し引きが全組み合わせで
正しいかを検証できない。**`test-jansou` が71,534件通っているのは、いまルールが一つだから。

**3〜4種類の「卓」として束ねる。**

- 標準ルール — 喰いタンあり・赤あり。大半の大会
- 競技ルール — 喰いタンなし・赤なし・一発裏なし。格の高い大会
- 祝儀ルール — 赤多め・割れ目あり。雀荘の日常

プレイヤーは組み合わせを選ばず「今日はどの卓か」を知るだけ。
**検証すべき組み合わせが3〜4に収まり、臨場感はむしろ強くなる。**

**ルールの表示**

麻雀のルール差は**打ち方が変わる**ので、対局が始まってから調べるものではない。
**卓に着く前に出すこと。**`taikai.js` の「対局の設定」の隣に「この卓のルール」を常設し、
**標準と違う点だけ**を目立つ色で出す。「喰いタンなし」の一行があれば打ち方が変わる。
押して詳しい説明が出るのは、そのうえでの補助。

**動かす前に：**`RULES_DEFAULT` は**いまの `engine.js` の実際の振る舞いを写したもの**で、
理想の既定値ではない（喰いタンあり・赤3枚・一発裏あり・割れ目なし・ハコ下で終了）。
分岐を入れるときは、**まず既定のまま通ることを確かめてから**片方ずつ倒すこと。

### pop / favor / local の調査 → **調査は済んだ（2026年9月7日）**

指示書 `INSTRUCTIONS_pop_favor_survey.md` はリポジトリに残っていません。
調査そのものは通したので、以下が**いまの事実**です。

**`favor` は動いている。**貯まる経路は6つ：

| 経路 | 量 |
| --- | --- |
| 雀荘のゲスト来店「もてなす」（30,000円） | +15 |
| 雀荘のゲスト来店「ふつうに接客」 | +4 |
| 遠征中の留守にゲスト来店（既定で接客） | +4 |
| 遠征中の留守に荒らし → 留守番が打った | +2 |
| 遠征の「口説く」で一局打つ | 勝ち +12 / 負け +5 |
| アイドル案件（送った子ぜんいん） | +2〜+5 |

**上限100・減る経路は無い**（`Math.min(100, …)` だけ）。
`Scout.costOf()` が `契約金 = 素の額 × (1 − favor/200)` で読むので、
**満額100で半額。S級なら 300万 → 150万で、最大150万円の値引き**。
`favor` は下がらないので、**通えば通うほど一方的に安くなる**。
バランス再測定（下）のときに、この一方通行を含めて見ること。

`favor`（人）と `local`（土地）の住み分けは付いています。
遠征で一局打つと**両方**上がりますが、効き先が違う
（`favor` は契約金、`local` は雀ドルの出やすさと誘われやすさ）。
`office.js` の 急所コメントが根拠です。

**表示も入った（2026年9月7日）。**値引きが効いていることが三画面に出ます
——**名鑑の「契約金の目安」・スカウトのカード・遠征の交渉**で、同じ一行：

```
契約金  2,250,000円
        好感度 50 → 25%引き（素の額 3,000,000円）
```

文面を組むのは `Scout.discountOf` 一つ、見た目は `style.css` の `.costOff` 一つ。
`favor` が 0 のときは二行目を出しません。
名鑑には好感度の行が無いので、**この一行が好感度の初出**になります
（`favor` の数字そのものは事務所の名簿シートに出ます）。

### イベント固定6人 → **解放済み**

`contract: 'event'` の6人は `offers.js` の `CONTRACT_OFFERS` で開いています。
`RULES.event` が見るのは**「その依頼を受けたか」だけ**で、条件は依頼側にあります
（`office/spec.md` §8.2。両方に書かないこと）。

  好感度 ＋ 事務所ランク ＋ 大会実績 → 朝に話が届く → 受ける
  → その子の地方へ遠征して「口説く」→ 現地で勝つ → 契約

| 依頼 id | ID | 名前 | 好感度 | 事務所ランク | 優勝が要る大会 |
| --- | --- | --- | --- | --- | --- |
| `event-001` | 001 | 天城 リオ | 80 | 5 | 雀エイト選抜戦 |
| `event-008` | 008 | 朧 ゆかり | 70 | 4 | タイトル戦 |
| `event-016` | 016 | 真壁 きょうか | 60 | 3 | 全国オープン |
| `event-018` | 018 | 白鷺 香澄 | 50 | 3 | 全国オープン |
| `event-042` | 042 | 緋田 ましろ | 40 | 2 | 地方リーグ |
| `event-054` | 054 | 神保 ちとせ | 45 | 2 | 地方リーグ |

`prio: 9` かつ `once` なので、**本拠地の規模に依らず条件だけで必ず届きます**。
単体ページ（`scout.html`）は `offers.js` を読まないので、そこでは閉じたままです。

### `contract: 'pop'` の17人

キャラが074〜200まで入ったので**17人**です（以前の「11人」は古い数）。
判定するのは相手の人気ではなく**事務所の所属全員の人気の合計**
（`POP_NEEDED = { S: 480, A: 320, B: 190, C: 90, D: 40 }`。相手の級で引く）。

| 級 | 必要な合計 | 該当 |
| --- | --- | --- |
| S | 480 | 005 |
| A | 320 | 012・052・075・082・087 |
| B | 190 | 017・056・097・106・107・119 |
| C | 90 | 037・127・137・145・156 |

### アイドルデビュー → **入っている**

`pop` の供給源は `offers.js` の `IDOL_OFFERS` 4件で、`st.popUp` に貯まります
（元データの `pop` は書き換えない）。**上限はありません。**

| id | 名前 | 日数 | 人数 | pop | 報酬 | favor | 対局 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `idol-local-tv` | 地方局の深夜番組 | 1 | 1-2 | 2 | 30,000 | +2 | — |
| `idol-photo` | 雑誌のグラビア | 1 | 1 | 3 | 80,000 | +3 | — |
| `idol-event` | ファン対局会 | 1 | 2-3 | 4 | 50,000 | +4 | あり |
| `idol-tv-match` | テレビ対局 | 2 | 1 | 8 | 200,000 | +5 | あり |

性格（`chara`）が `fit` に合えば満額、外れると半分。対局付きの案件で1着なら倍。
発火は `roster.length >= 2` だけで、`once: false` なので繰り返し届きます。

**人気の読み口は `characters.js` の `popOf(st, chara)` ただ一つ**
（元データの `pop` ＋ `st.popUp[id]`）。`office.js` / `jansou.js` /
`scout.js` / `meikan.js` はこれを呼ぶこと。**式を書き写さないこと**——
以前 `scout.js` だけが底上げを見ておらず、`RULES.pop` が経路で割れて
**同じ子がスカウト画面では契約できず、遠征の交渉ではできる**状態でした
（2026年9月7日に直した。`tools/test-office.js` が機械的に見て固定している）。

### 全国マップ

---

## 決めごとが要るもの

### 定員の数え方

`Scout.roster()` は `st.contracted` だけを写すので、**プレイヤー自身が定員に数えられていません。**「所属N／定員M」という表示は、プレイヤーを含めて数えると読むほうが自然なので、実装のほうが直感に反しています。

仕様として正しいかは未決。**バランス再測定のときに一緒に判断する。**

### 14人ソフトキャップ

超線形の管理費加算で緩く抑える方針まで決まっていますが、実装は保留。後期の雀荘収益がかなり伸びるため、ハードキャップは避ける。

**動かす前に：**下のバランス再測定を先に。

### バランス再測定

遠征が日数を消費するようになったので、既存の数字（スカウト費用、雀荘の日次収益、三シナリオの較正）は測り直しが要ります。

**測り直しの範囲を広げないため、上の2件はこれより先に動かさないこと。**

---

## 効いてくるのが先のもの

### `renderGrid()` の全DOM再構築

200人でフィルタを切り替えるたびに全部組み直しています。実測147ms（170人のとき165〜171ms）なので、いまは体感で悪化していません。遅くなったら対応。

### `meikan.html:109` の `compGuess`

`{S:90, A:72, B:50, C:28, D:12}` で、`tournament.js` の `compFromRank` と **A/B が2ずつ違います**。単体ページの動作確認専用で本編には効きませんが、目視での誤判断を生みます。いずれ揃える。

### 残り174人の裏話

S/A/B級は書き下ろし、残りはモジュラー生成（4軸×約12種）の方針。初期メンバー10人の3章ぶんが最優先（全プレイヤーが選択画面で目にするため）、A級は2章ずつ。

---

## 画像の差し替え

上書きするだけなので、いつでも。差し替えるときは対応する `look` も同時に直すこと。

| ID | 名前 | 問題 |
|---|---|---|
| 172 | 神崎 うい | ラベンダーが銀白に。178と系統が近い |
| 186 | 菅原 あおな | ミントが灰に。カーディガンの下がキャミ |
| 193 | 立石 のえ | ジャケットがビスチェに |
| 174 | 京極 しの | 梅色が出ず黒に。191・200と着物3人が近い |
| 151 | 三隅 いよ | 藍の着物が出ず、167と見分けがつかない |
| 130 | 香坂 このは | 深緑の髪が黒に。片目を隠す前髪も出ず |
| 170 | 織田 くれは | 紅の髪が黒に |
| 094 / 104 | 稲垣ほのか / 白瀬みなも | ほぼ同じ絵 |
| 087 | 阿久津 まき | 下着姿 |
| 131・139・144・150・158 | | スポーツブラ単体 |
| 086 | 望月 さゆり | 30歳・白髪設定が出ず、089とほぼ同じ |

プロンプトを書くときの実測メモ（色が落ちる書き方、上着の名詞、着物の書式）は `HANDOFF_NEXT.md` にあります。171〜200では `vividly dyed` / `brightly dyed` と `silver grey` への統一で色落ちを0件にできました。
