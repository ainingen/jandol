# 保留リスト

いま手をつけないと決めたもの。着手するときは、それぞれの「動かす前に」を先に読むこと。

---

## 後回しにしないもの

**`78f6807` の `main` へのマージ。** `comp` の入り口を揃えたコミットが `claude/add-characters-074-170-cj31ob` に残ったままです。200人ぶんのデータは `c57d181` で `main` に入っていますが、この1本だけ未マージです。

放っておくとブランチが古びて、あとで取り込むほど衝突の可能性が上がります。麻雀UIの区切りがついたところで、単体で通してください。作業自体は数分です。

---

## 対局画面で外したもの（`docs/design/match/spec.md` §9）

8段はすべて入った。次の四つは意図して外してある。

### `render()` 全体の keyed 化

keyed なのは手牌と四つの河だけ。席プレート・コンパス・吹き出しは innerHTML で組み直している。
いまのところ体感で困っていない。**動かす前に：**牌の `Map<id,node>` の作法（`spec.md` §3.3）に揃える。

### 和了画面の作り直し

`#overlay .panel` は忍雀のまま（配色だけ寄せた）。卓が固まったので、別途。

### 点棒の移動アニメ

段3の仕組み（keyed ＋ FLIP）の上に後から足せる。点棒のノードを持てばよい。

### ボイス

200人ぶんは現実的でない。効果音だけ。
**鳴る12本は ElevenLabs の生成音に差し替え済み**（2026年9月4日）。
差し替えは `audio_raw/` に置いて `tools/prep-sfx.py` を回すだけ（`SOURCES` に行を足す）。
`audio/LICENSE.txt` の出典欄も一緒に直すこと。

---

## 他の作業を待っているもの

### pop / favor / local の調査と画面表示

指示書は `INSTRUCTIONS_pop_favor_survey.md` に書いてあります。**A7-3（`local` で「話す」の踏み込みが三段になる）より前に調査を通す**という前提で書いてあるので、A7-3が先に入った場合は前提を読み直してください。

知りたいことの中心は2つ。

- `favor` は `Scout.costOf()` が契約金の値引きに使っているが、**実際に加算される経路が動いているか**。貯まらないなら値引きは死んだ機能で、貯まっているならプレイヤーに何も見えないまま契約金が下がっている
- `favor`（契約金の値引き）と `local`（会話の踏み込み）は**どちらもプレイヤーと相手の距離を表す数字**で、住み分けがついているか。同じ行動で両方上がるなら、プレイヤーには「なぜか数字が2つ動く」としか見えない

**動かす前に：**麻雀UIが `office.js` / `jansou.js` に触っているなら、そこと重なります。

### イベント固定6人の解放

`contract: 'event'` の6人（No.001 天城リオほか）が契約不能のまま。`offers.js` の依頼テーブルに載せることで開く設計になっています。`favor` の蓄積が条件に入るので、**上の調査より後**。

### `contract: 'pop'` の11人

`pop` を上げる手段（アイドル業）が実装されるまで実質契約不能。想定内の詰まりです。アイドルデビューの実装待ち。

### アイドルデビュー

`pop` を上げる唯一の手段になる予定。これが入るまで `pop` は増えません。

### 全国マップ

---

## 決めごとが要るもの

### 定員の数え方

`Scout.roster()` は `st.contracted` だけを写すので、**プレイヤー自身が定員に数えられていません。**「所属N／定員M」という表示は、プレイヤーを含めて数えると読むほうが自然なので、実装のほうが直感に反しています。

仕様として正しいかは未決。**バランス再測定のときに一緒に判断する。**

### 14人ソフトキャップ

超線形の管理費加算で緩く抑える方針まで決まっていますが、実装は保留。後期の雀荘収益がかなり伸びるため、ハードキャップは避ける。

**動かす前に：**下のバランス再測定を先に。

### バランス再測定

遠征が日数を消費するようになったので、既存の数字（スカウト費用、雀荘の日次収益、三シナリオの較正）は測り直しが要ります。

**測り直しの範囲を広げないため、上の2件はこれより先に動かさないこと。**

---

## 効いてくるのが先のもの

### `renderGrid()` の全DOM再構築

200人でフィルタを切り替えるたびに全部組み直しています。実測147ms（170人のとき165〜171ms）なので、いまは体感で悪化していません。遅くなったら対応。

### `meikan.html:109` の `compGuess`

`{S:90, A:72, B:50, C:28, D:12}` で、`tournament.js` の `compFromRank` と **A/B が2ずつ違います**。単体ページの動作確認専用で本編には効きませんが、目視での誤判断を生みます。いずれ揃える。

### 残り174人の裏話

S/A/B級は書き下ろし、残りはモジュラー生成（4軸×約12種）の方針。初期メンバー10人の3章ぶんが最優先（全プレイヤーが選択画面で目にするため）、A級は2章ずつ。

---

## 画像の差し替え

上書きするだけなので、いつでも。差し替えるときは対応する `look` も同時に直すこと。

| ID | 名前 | 問題 |
|---|---|---|
| 172 | 神崎 うい | ラベンダーが銀白に。178と系統が近い |
| 186 | 菅原 あおな | ミントが灰に。カーディガンの下がキャミ |
| 193 | 立石 のえ | ジャケットがビスチェに |
| 174 | 京極 しの | 梅色が出ず黒に。191・200と着物3人が近い |
| 151 | 三隅 いよ | 藍の着物が出ず、167と見分けがつかない |
| 130 | 香坂 このは | 深緑の髪が黒に。片目を隠す前髪も出ず |
| 170 | 織田 くれは | 紅の髪が黒に |
| 094 / 104 | 稲垣ほのか / 白瀬みなも | ほぼ同じ絵 |
| 087 | 阿久津 まき | 下着姿 |
| 131・139・144・150・158 | | スポーツブラ単体 |
| 086 | 望月 さゆり | 30歳・白髪設定が出ず、089とほぼ同じ |

プロンプトを書くときの実測メモ（色が落ちる書き方、上着の名詞、着物の書式）は `HANDOFF_NEXT.md` にあります。171〜200では `vividly dyed` / `brightly dyed` と `silver grey` への統一で色落ちを0件にできました。
