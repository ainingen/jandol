# CLAUDE.md

「雀ドル発掘放浪記」— 雀ドルを発掘・スカウトして事務所を大きくし、
大会で賞金と名声を得ていくブラウザゲーム。

## まずこれを読む

このプロジェクトの決めごとは、次に書いてある。作業を始める前に読むこと。

- **`README.md`** … ファイル構成、ビルドの手順、単体ページの使い方、
  数値を触るときの置き場所の対応表、実対局・列レイアウト・字体・牌の絵・
  サムネイルの仕組みと制約、PLiCyの制約
- **`docs/HANDOVER.md`** … 設計の経緯と判断の根拠、調整済みの数値、
  ハマった罠、次の一手
- **`docs/design/jansou/`** … 直営雀荘の設計一式。リニューアルの指示書
  （`spec.md`）、卓の自由配置と隣接コンボ（`placement.md`）、名前生成の
  参照実装（`names.py`）、配色とレイアウトの正となるモックアップ画像。
  雀荘まわりを触るときはここを読む

罠は再発しやすいものが `docs/HANDOVER.md` §5 にまとまっている。
数値を動かす前に、その根拠が §4 に書かれていないか確認すること。

## ビルド

`src/` を編集したら、リポジトリ直下で必ず実行する。

```
python3 build.py            index.html は約13KB
```

`index.html` は `shell.html` に `src/` を指す `<link>` と `<script>` を
差し込むだけのもの。CSS 11本と JS 16本はすべて `src/` のまま読まれる。
**PLiCyで外部のCSS・JSが読めることは確認済み**なので、
以前あった一枚版（`--single`）は廃止した。

- **`index.html` を直接編集しないこと。** ビルド結果なので、次のビルドで消える。
  直すのは必ず `src/` 側
- **配布物には `src/` を必ず含めること。** 分割版は `src/*.css` と `src/*.js` を
  読みに行く。含め忘れると真っ白な画面になる
- **`index.html` は 500KB 以下に保つこと。** 配布先の PLiCy に上限がある。
  `build.py` が毎回サイズを確認し、超えたらエラーで止まる。
  いまは13KBなので、`shell.html` に直接書き足しすぎないかぎり掛からない
- **確認は `python3 -m http.server` 越しに開くこと。** `src/` を
  相対パスで読むので、ページ単体を切り出して開く経路では読み込めない
- **表紙の高さと、アプリ内スクロールを壊さないこと。** PLiCyのプレイヤーは
  枠のスクロールを止めていることがある。`body` は `100dvh` 固定で、
  流すのは `#scroll` の中だけ。表紙は `max-height` で縮めてボタンを
  画面内に残している（`docs/HANDOVER.md` §5）

画面ごとに単体で確認したいときは、直下の `meikan.html` `team.html`
`taikai.html` `scout.html` `jansou.html` を開く。ビルド不要で `src/` を直に読む。

## 直営雀荘のリニューアル（2026年9月・全5段階 完了）

`docs/design/jansou/spec.md` の5段階はすべて実装済み。到達点：

- 「今日の営業をはじめる」でフロアに一日が流れる。スキップ・倍速（×1/×2/×4）
- **スキップしても再生しても、収支・成長・評判が完全に一致する**
- 客をタップすると誰か分かり、通うほど名前が分かっていく（4段階）
- ボトル勝負を受けると実対局に入り、戻って再生が続く
- 内装・卓の型・宣伝を買い足すと、フロアの絵がそのぶん増える

### 構造（ここを外すと全部が崩れる）

**帳簿層 → 演出層 → 表示層**の三層に分けてある（`spec.md` §5.4）。

- **帳簿層**（`JansouFloor.build`）… 純関数。帯ごとに Σ`arrive.count` ＝ `guests`、
  Σ`pay.amount` ＝ `sales` を**厳密に**満たすタイムラインを組む
- **演出層** … 各到着を「歩いて入る」か「席で入れ替わる」か、**ビルド時に**決めて
  `mode` に持つ。再生層は読むだけで乱数を引かない
- **表示層** … 速度で変わるのはここだけ（ポップの束ね方、ティッカーの量）

`runDay` は **prepareDay（乱数を全部使い切る）→ playDay（割り込みで ask／実対局）
→ settle（純関数）** の三段。**乱数を再生層に持ち込むと結果一致が壊れる。**

## 卓の自由配置と隣接コンボ（2026年9月・全3段階 完了）

`docs/design/jansou/placement.md` の3段階も実装済み。到達点：

- 卓と設備を **8px のマス目**（24列×15行）に自分で置ける（「模様替え」）。
  卓の足元は**席4つを含む 7×5 マス**なので、置けた卓には必ず4席が描ける
- 既存セーブは `normalize()` が `autoPlace` で拾い、いままでと同じ絵になる。
  一度でも動かすと `floor.auto: false` になり、以後は勝手に動かない
- 隣り合わせで**コンボ6つ**（くつろぎ席・カウンター席・入口席・静かな席・
  花道・ラウンジ）。模様替え中は成立が札で見え、つまんでいる間に
  「ここに置くと ○○ が成立します」と先に出る

### 配置まわりの急所（ここを外すと崩れる）

- **コンボは `computeDay()` に触らない。** 動かすのは「誰が座るか・
  どれだけ居るか・誰から立つか・チップ・ボトルの格・常連の進み・指名」だけで、
  `guests` と `sales` は1人・1円も変わらない
- **チップは `pay` ではなく `bonus` の経路。ここを `pay` にすると
  帯の Σ`pay` ＝ `sales`（`spec.md` §5.3）がその場で崩れる。**
  `build` が `bonus{label:'チップ'}` を置いて `summary.tips` に合計を持ち、
  `prepareDay` が控えて **`settle` が臨時収入に足す**。再生層は1円も動かさない
- **論理フロアは 200×164 で固定。** 端末の幅で変えないこと。
  狭いときは横に送って見る（配置は帳簿に効くので、端末で変わってはいけない）

### テスト

`node tools/test-jansou.js` … 71,448件。DOMに触らない関数だけを見る。
描画そのものはブラウザ検証（`spec.md` §13）。

## 次にやること

優先順。上ふたつは他と独立していて、下ふたつは `computeDay()` の再計測が要る。

1. **月末決算・月報**（`spec.md` §15「第三段」）。日々の営業は締まったが、
   月でまとめて見るものが無い
2. **客タイプ21種の効果**。実装したのは荒らし・常連の主・推しファン（見た目のみ）の
   3つだけで、残りは `effect` を定義してあるが**どこからも読まれていない**（§6.2）。
   **配置コンボと同じく、`computeDay()` に触らない経路（誰が座るか・臨時収入・
   常連の進み・buffs）で効かせられるものから順に**入れるのが安全
3. **推しファンの売上への反映**。`computeDay()` の入力に `favor` を足す形になるが、
   `docs/HANDOVER.md` §4 の調整済み数値が動くので**2000日の再計測とセット**で行う
4. **`槓` `發` `簪` のフォント欠け**（下記）

置ける小物を増やす（コンボの種類を足す）のは、いまの6つが手に馴染んでから。

## 既知の課題

- ~~狭い幅（2列配置）では、卓の上の2席に座った客が描かれない~~
  **→ 直った（2026年9月）。**卓の足元に席4つぶんを含めたので、
  置けた卓には必ず4席がある。狭い幅では横に送って見る。

- **`槓` `發` `簪` の3字が丸ゴシックに無い。** 元書体（M PLUS Rounded 1c）自体に
  収録がないので、`tools/make-font.py` を回しても直らない。
  対局画面に出る字である（役名「槍槓」「四槓子」、役牌の「發」。
  `簪` は `characters.js` のキャラ説明）。いまはこの3字だけブラウザの
  代替書体で表示されている。直すなら、`maru.css` の `font-family` に
  丸ゴシック系のフォールバックを明示するか、`發` → `発`のように
  字を差し替えるかのどちらか。
