# CLAUDE.md

「雀ドル発掘放浪記」— 雀ドルを発掘・スカウトして事務所を大きくし、
大会で賞金と名声を得ていくブラウザゲーム。

## まずこれを読む

このプロジェクトの決めごとは、次に書いてある。作業を始める前に読むこと。

- **`README.md`** … ファイル構成、ビルドの手順、単体ページの使い方、
  数値を触るときの置き場所の対応表、実対局・列レイアウト・字体・牌の絵・
  サムネイルの仕組みと制約、PLiCyの制約
- **`docs/HANDOVER.md`** … 設計の経緯と判断の根拠、調整済みの数値、
  ハマった罠、次の一手
- **`docs/design/jansou/`** … 直営雀荘の設計一式。リニューアルの指示書
  （`spec.md`）、卓の自由配置と隣接コンボ（`placement.md`）、名前生成の
  参照実装（`names.py`）、配色とレイアウトの正となるモックアップ画像。
  雀荘まわりを触るときはここを読む
- **`docs/design/office/spec.md`** … 事務所をハブにして日進行を一本化する
  指示書（全5段階・完了）。県と地方の二層、遠征、届く依頼、疲労と調子。
  事務所・日の進みかた・スカウト・大会を触るときはここを読む
- **`docs/design/office/room.md`** … 事務所を部屋にする設計（A6）。
  **いまの「次の一手」はここ。第一段（部屋と枠、シート）は入っている。**
  見取り図、380px のレイアウト、いまの朝の画面の行き先の対応表、
  夜の日報の置き場所、段階の切り方
- **`docs/design/scout/spec.md`** … スカウトの中身（A4.5）。遠征先の雀荘を
  静止画で見せ、打ち筋を癖として床に出し、交渉に段階を入れる。
  **三段すべて入っている。**スカウトまわりを触るときはここを読む
- **`docs/ROADMAP.md`** … その先まで含めた全体の構想と順番を一枚に。
  なぜこの順なのか、何を[A]〜[G]のどこに置いたか、再測をいつやるか。
  **決めごとが増えて全体が見えなくなったときに読み直す**

罠は再発しやすいものが `docs/HANDOVER.md` §5 にまとまっている。
数値を動かす前に、その根拠が §4 に書かれていないか確認すること。

## ビルド

`src/` を編集したら、リポジトリ直下で必ず実行する。

```
python3 build.py            index.html は約13KB
```

`index.html` は `shell.html` に `src/` を指す `<link>` と `<script>` を
差し込むだけのもの。CSS 11本と JS 16本はすべて `src/` のまま読まれる。
**PLiCyで外部のCSS・JSが読めることは確認済み**なので、
以前あった一枚版（`--single`）は廃止した。

- **`index.html` を直接編集しないこと。** ビルド結果なので、次のビルドで消える。
  直すのは必ず `src/` 側
- **配布物には `src/` を必ず含めること。** 分割版は `src/*.css` と `src/*.js` を
  読みに行く。含め忘れると真っ白な画面になる
- **`index.html` は 500KB 以下に保つこと。** 配布先の PLiCy に上限がある。
  `build.py` が毎回サイズを確認し、超えたらエラーで止まる。
  いまは13KBなので、`shell.html` に直接書き足しすぎないかぎり掛からない
- **確認は `python3 -m http.server` 越しに開くこと。** `src/` を
  相対パスで読むので、ページ単体を切り出して開く経路では読み込めない
- **表紙の高さと、アプリ内スクロールを壊さないこと。** PLiCyのプレイヤーは
  枠のスクロールを止めていることがある。`body` は `100dvh` 固定で、
  流すのは `#scroll` の中だけ。表紙は `max-height` で縮めてボタンを
  画面内に残している（`docs/HANDOVER.md` §5）

画面ごとに単体で確認したいときは、直下の `meikan.html` `team.html`
`taikai.html` `scout.html` `jansou.html` を開く。ビルド不要で `src/` を直に読む。

**スカウトは下のタブから外した**（探しに出るのは遠征なので、入口は事務所ひとつ）。
画面そのものは残っている——**見つけた子と契約できるのはここだけ**なので、
事務所の「スカウト」から入る。`scout.html`（単体ページ）もそのまま。

## 自分で遊んで確かめる（開発用の入口）

**`debug.html` を開く。**金・所属・評判・設備が揃ったセーブを書いて、
そのまま本編に入れる。フロア・設備・隣接コンボは条件が揃わないと画面に出ないので、
最初から遊んで揃えるかわりにここで作る。

```
python3 -m http.server 8000
→ http://localhost:8000/debug.html
```

釦は二つ。

| | 中身 | 何を見るのに使うか |
| --- | --- | --- |
| **遊べる状態を作る** | 1000万円・8人契約・評判45・卓4／内装3 | 設備を自分で買い足す手触り |
| **全部盛りにする** | 1000万円・14人契約・評判80・卓6／内装5 | フロアの賑わい、上客・特別な客、ボトル勝負 |

書いたあと **`index.html` を開き、下のタブの「雀荘」**を押す。
「今日の営業をはじめる」でフロアが流れる。設備は雀荘の画面の下、
隣接コンボは**「模様替え」**から。

**全部盛りでも成立しているコンボは3つだけ**（くつろぎ席・カウンター席・入口席）。
`autoPlace` はソファとカウンターを隣に置かないので、
**ラウンジ・花道・静かな席は模様替えで自分で組む。**

`jansou.html`（雀荘の単体ページ）にも同じ釦を「遊べる状態」「全部盛り」として置いてある。

### 本番に混ぜないための決めごと

- **`build.py` の `JS` リストに `debug.js` を足さないこと。** 足すと
  `index.html` に入り、普通のプレイヤーにデバッグの入口が見える。
  `build.py` は毎回、本番に混ざっていないかを確かめて、混ざっていたら止まる
- **配布ZIPからは `debug.html` と `src/debug.js` を外すこと**（`README.md` の配布の手順）。
  ビルドには入らないが、ZIPに混ぜるとURLを知っている人には届く
- GitHub Pages はリポジトリをそのまま配るので、`debug.html` は
  **URLを知っていれば開ける**。どこからもリンクしていないだけで、隠してはいない

## 直営雀荘のリニューアル（2026年9月・全5段階 完了）

`docs/design/jansou/spec.md` の5段階はすべて実装済み。到達点：

- 「今日の営業をはじめる」でフロアに一日が流れる。スキップ・倍速（×1/×2/×4）
- **スキップしても再生しても、収支・成長・評判が完全に一致する**
- 客をタップすると誰か分かり、通うほど名前が分かっていく（4段階）
- ボトル勝負を受けると実対局に入り、戻って再生が続く
- 内装・卓の型・宣伝を買い足すと、フロアの絵がそのぶん増える

### 構造（ここを外すと全部が崩れる）

**帳簿層 → 演出層 → 表示層**の三層に分けてある（`spec.md` §5.4）。

- **帳簿層**（`JansouFloor.build`）… 純関数。帯ごとに Σ`arrive.count` ＝ `guests`、
  Σ`pay.amount` ＝ `sales` を**厳密に**満たすタイムラインを組む
- **演出層** … 各到着を「歩いて入る」か「席で入れ替わる」か、**ビルド時に**決めて
  `mode` に持つ。再生層は読むだけで乱数を引かない
- **表示層** … 速度で変わるのはここだけ（ポップの束ね方、ティッカーの量）

`runDay` は **prepareDay（乱数を全部使い切る）→ playDay（割り込みで ask／実対局）
→ settle（純関数）** の三段。**乱数を再生層に持ち込むと結果一致が壊れる。**

## 卓の自由配置と隣接コンボ（2026年9月・全3段階 完了）

`docs/design/jansou/placement.md` の3段階も実装済み。到達点：

- 卓と設備を **8px のマス目**（24列×15行）に自分で置ける（「模様替え」）。
  卓の足元は**席4つを含む 7×5 マス**なので、置けた卓には必ず4席が描ける
- 既存セーブは `normalize()` が `autoPlace` で拾い、いままでと同じ絵になる。
  一度でも動かすと `floor.auto: false` になり、以後は勝手に動かない
- 隣り合わせで**コンボ6つ**（くつろぎ席・カウンター席・入口席・静かな席・
  花道・ラウンジ）。模様替え中は成立が札で見え、つまんでいる間に
  「ここに置くと ○○ が成立します」と先に出る

### 配置まわりの急所（ここを外すと崩れる）

- **コンボは `computeDay()` に触らない。** 動かすのは「誰が座るか・
  どれだけ居るか・誰から立つか・チップ・ボトルの格・常連の進み・指名」だけで、
  `guests` と `sales` は1人・1円も変わらない
- **チップは `pay` ではなく `bonus` の経路。ここを `pay` にすると
  帯の Σ`pay` ＝ `sales`（`spec.md` §5.3）がその場で崩れる。**
  `build` が `bonus{label:'チップ'}` を置いて `summary.tips` に合計を持ち、
  `prepareDay` が控えて **`settle` が臨時収入に足す**。再生層は1円も動かさない
- **論理フロアは 200×164 で固定。** 端末の幅で変えないこと。
  狭いときは横に送って見る（配置は帳簿に効くので、端末で変わってはいけない）

### テスト

`node tools/test-jansou.js` … 71,534件。DOMに触らない関数だけを見る。
描画そのものはブラウザ検証（`spec.md` §13）。

`node tools/drive-jansou.js --days 30` … ブラウザで実際に営業を回す。
30日で月報が出るか、割り込みで詰まらないかは押してみるしかない。
**押す順は「ポップアップが先、スキップは後」**（`monthly.md` §13）。

## 月末決算・月報（2026年9月・完了）

`docs/design/jansou/monthly.md` の設計どおり実装済み。到達点：

- **30日で締めて月報が出る**（`MONTH_DAYS`）。営業と地続きで、日報の次に一枚
- 時間帯別の場代の帯、収支の帯、客数と常連の育ち、いちばん客を呼んだ子、
  できごと、評判の推移。雀荘の画面から**読み返せる**（保存済みの月報を開き直すだけ）
- **経済には触れていない。**`HANDOVER.md` §4 の実測がそのまま生きている

### 決算まわりの急所（ここを外すと崩れる）

- **締めは `settle` の末尾＝純関数の中。** `playDay` に置くと
  「スキップしても再生しても完全一致」（`spec.md` §16）が壊れる
- **`normalize()` に `month` / `months` を書き忘れると毎日消える。**
  この関数は既知のキーだけで組み直すので、列挙し忘れた `parlor` のキーは落ちる。
  症状は「月報は出るが全部0」
- **場代は独立して持たない。帯の合計として定義する**（`spec.md` §5.3 と同じ形）。
  チップとボトルは臨時収入。**場代の帯に混ぜない**
- **`parlor.log` から月を集計しない。** `.slice(-7)` で7日ぶんしか無い
- **月報は span だけで組む。** `<p>` の中に入るので `<div>` を混ぜると
  パーサが `<p>` を閉じ、見た目だけ崩れる（数字は合うので気づきにくい）
- **月末は締めであってリセットではない。** 空にするのは `parlor.month` だけ。
  評判・buffs・ボトル在庫・常連・配置は持ち越す
- **期の番号を `months.length + 1` で出さない。** `months` は直近12期で
  打ち切るので、13期目以降ずっと「第13期」になる。`nextMonthNo()` を使う

## 事務所ハブと日進行の統一（2026年9月・全5段階 完了）

`docs/design/office/spec.md`。**雀荘だけが日単位で動き、大会とスカウトは
回数単位で動いている**という二本立ての時間軸を、一本にしていく。
`docs/design/jansou/spec.md` §15 が「今回の範囲外」とした
「連続時間化」そのもの。

**第五段まで済んでいる。**第四段までの到達点：

- 表紙から入ると**事務所**に着く。一日が朝→昼→夜→朝と回る
- 新規開始で**事務所名と本拠地の県**を決める（`title.js`）。
  既存セーブは事務所を初めて開いたときに一度だけ県を聞かれる
- **営業開始の入口は事務所だけ**（「今日を始める」）。
  雀荘の画面は設備とシフトを整え、再生と結果を見る場所になった
- 47都道府県のデータ（`src/geo.js`）。座標・規模・所属地方と、
  遠さの段階 0〜5。**`aisho` と `area` は地方のまま**で、県はその上の層
- 所属一覧に **`pop` と `favor` が初めて出た**
- **店が無い日も日は進む。**開店資金50万を貯めているあいだも一日が過ぎ、
  所属の日当は出ていく。**「まず店を持つ」が序盤の最初の節目**
- **配置（`assign`）**。所属の一人ずつに「店」か「休み」を持たせる。
  遠征（`trip`）と依頼（`job:<id>`）の器も切ってあり、**配置は変えるまで続く**
- **雀荘のシフト表（昼・夕・夜）を事務所へ移した。**配置と同じ行に並ぶ。
  **保存の形は変えていない**（`parlor.shifts` のまま）
- **疲労（`fatigue`）と調子（`cond`）が動く**（A5）。所属一覧に帯で出る
- **遠征**。行き先の県・目的（探す／口説く）・同行者0〜3人を決めて出る。
  日数は `2 + far`、費用は `SCOUT_COST × (1+far) × (1+同行者数)`。
  滞在した日数ぶん探せ、口説くなら**現地で一局打って勝てば契約の話**に進む。
  **負けても永久失敗にはならない**（好感度が積まれる）
- **留守番**。代表戦は `deputyOf` の子が `simulateTable` で打ち、
  `joinNight` が効かない。**日ごとの再生はしない**で、帰った日の夜にまとめて出す
- `scout.js` の「探す」は遠征に置き換わった（単体ページには残る）
- **届く依頼**（`src/offers.js`・15件）。大会の招待5・契約イベント6・アイドル案件4。
  **発火は条件だけで、日付を見ない。**受けるか見送るかを朝に決める
- **大会は依頼から入る。**選択画面を通らず直行し、`days` を消費して事務所へ返る
- **契約イベント6人が解けた。**好感度＋事務所ランク＋大会実績で話が届き、
  受けて遠征で会いに行って勝てば契約。**天城リオ No.001 が図鑑に入る**
- **アイドル活動**が `pop` の供給源（`popUp`）。演出は作らず、日報一行とセリフだけ

**大会・スカウト・アイドル活動が、すべて同じ `parlor.day` を消費する。**
時間軸は一本になった。**第五段（疲労・調子・雀エイト表）も済んで、五段階は全部閉じた。**

### 事務所まわりの急所（ここを外すと崩れる）

- **`parlor.day` を進めるのは `Jansou` の `settle()` の中**（昼の終わり）。
  ここは「スキップしても再生しても完全一致」が乗っている純関数なので触らない。
  **事務所の「明日へ」は日を進めない。**夜を畳んで朝へ戻すだけ
- **新しい日カウンタを作らない。**`parlor.day` を全画面がそのまま読む。
  `MONTH_DAYS` と `wageOf()` の30は対なので、別に持つと月報がずれる
- **事務所のセーブは最上位に持つ。`parlor` の下ではない。**
  `Jansou.normalize()` は知らないキーを捨てるため（下の「決算まわりの急所」と同じ罠）。
  既定値は **`shell.html` の `blankState()` と `loadState()`** に書く。
  **`store.onStart`（最初からはじめる）にも書き足すこと。**
  `title.js` が `onStart` の直前に `store.set` で書いているので、
  拾い忘れるとその場で消える
- **画面の切り替えは `shell.html` 末尾の `go(name, opts)` 一つだけ。**
  `index.html` はビルド結果なので触らない。朝と夜は `office.js` の内部状態で、
  `go()` には段階を持ち込んでいない
- **`officePref` の既定を勝手に決めない。**本拠地は一度きりの選択なので、
  無ければ聞く。`null` のままにしておくのが「まだ聞いていない」の印
- **店が無い日も日は進む**（`office/spec.md` §1.2）。
  昼を雀荘へ委譲せず、事務所の中で `Jansou.runClosedDay()` が
  **卓0で同じ締めを通す**（客0・売上0・家賃0で、日当だけが引かれる）。
  日を進める場所を `settle` 以外に増やさないため
- **`computeDay` は `tables <= 0` で早期に返す。**下の
  `Math.max(1, ...)` は「閉鎖した卓が全部を食っても一卓は残す」ための
  クランプなので、そこへ落とすと**卓0でも4席ぶんの客が湧く**。
  営業日は `normalize()` が `tables` を最低2に丸めるので、ここを通らない
- **`settle` はモジュール層にある**（`mount` の閉包ではない）。
  純関数なので node のテストから直に呼べ、店が無い日の締めも同じ関数を通る
- **シフトの移設はUIだけ。データは動かしていない**（`office/spec.md` §6.3）。
  保存先は `parlor.shifts` のままで、書くのは **`Jansou.setShift` 一つだけ**。
  事務所と雀荘の単体ページが同じ関数を通るので、既定値（夜だけ）の解釈が割れない。
  **`Jansou.normalize()` にも `assign` にも触れない**
- **日当は契約基準。**出勤の有無に関係なく、所属の全員に毎日払う
  （`office/spec.md` §6.3・§7.2）。`rest` にしても日当は浮かない。
  **HANDOVER §4 の三局面は動いていない**（どれも全員が出勤しているため。
  `node tools/measure-jansou.js` で前後の完全一致を確認済み）
- **所持金に下限を付けないこと。**マイナスは「給料の遅配」として許す。
  `settle` に `Math.max(0, ...)` を足すと、遅配が起きていること自体が
  見えなくなる。離脱の警告に繋げるのは `ROADMAP.md` [C]
- **`assign` の既定は「店」。知らない値も「店」に落とす。**
  あとから配置の種類が増えても、古いコードが読んだときに
  誰かが永久に出勤しなくなったりしないため。
  `trip` は `st.trip` が無ければ、`job:<id>` は受けた依頼でなければ「店」に戻す
- **出勤可能者を絞るのは `prepareDay` の入口の一箇所だけ**（`Office.parlorRoster`）。
  出勤者が減れば `computeDay()` の入力が減る。**新しい項は足していない**
- **`jansou.js` は `Office` を「あれば使う」。**単体ページで office.js が
  読まれていなくても「全員が店に立つ」＝いままでどおりに落ちる
- **遠征中の昼は雀荘へ委譲しない**（`office/spec.md` §7.5）。
  `Jansou.runAwayDay()` がフロアもポップアップも出さずに一日を回し、
  合計を `trip.store` に積んで、帰った日の夜にまとめて出す。
  締めは営業日と同じ `settle` を通る
- **留守の日に選べないぶんは既定で倒す**（代理が現場で判断した、という扱い）。
  ゲストは普通に接客、卓の故障は払えるなら修理、荒らしは留守番が打つ。
  **プレイヤーは受けるかどうかも選ばない**（§7.4）
- **`assign` の `trip` は「生きている遠征か」で見る**（`Office.tripOf`）。
  `st.trip` の有無だけで見ると、残り0日で置き去りになった trip が
  同行者を永久に出勤から外す
- **一回の費用の正は `Scout.SCOUT_COST` 一つだけ。**`planTrip` は
  そこに倍率を掛けるだけで、数を書かない
- **依頼の `when` に日付を書かない**（`office/spec.md` §1.3・§8.1）。
  見ていいのはプレイヤーの状態だけ。`parlor.day` を見ると、あとから足した
  依頼が100日進めた人には二度と届かなくなる。
  `tools/test-office.js` が `when` の中身を機械的に見て固定している
- **`RULES.event` は「依頼を受けたか」だけを見る**（§8.2）。
  条件そのもの（好感度＋事務所ランク＋大会実績）は `offers.js` に書く。
  両方に書くと二重になり、片方を直したときに必ずずれる
- **画面をまたぐ結果は `opts` で渡す。**大会から事務所へ戻るとき、
  `Office` は組み直されるので、**前の mount の閉包に控えた関数は宙に浮く**
  （呼んでも何も出ない）。`shell.html` の `goTaikai` を見ること
- **依頼に出ている子は、`assign` ではなく引数で出勤から外す**
  （`runAwayDay` の `busyIds`）。依頼はその場で片づくので、
  セーブに `job:` を書くと**古びたまま残る**危険のほうが大きい。
  **日当の母数からは外さない**（契約基準なので）
- **人気は元データ ＋ セーブの `popUp`。**`characters.js` の `pop` は
  書き換えない。`office.js` の `rosterOf` と `jansou.js` の `roster()` が
  **同じ読みかたをすること**——ここがずれると、事務所の表示と客足が食い違う

### テスト

`node tools/test-office.js` … 390件。47県の `region` が `REGIONS` に
一致するか、遠さの段階、事務所名の既定、配置の既定と落としかた、
遠征の日数と費用、留守番の選びかた、依頼の発火と `RULES.event`、
店が無い日の締め、セーブの前方互換。**雀エイト表**は、八人ぶん出ること・
強さの降順・始まりが S級だけになること（強さだけで並べると崩れること）・
同じセーブなら並びが揺れないこと・育てて勝つと上がること・
実績が所属にだけ乗ること・元データを書き換えないこと・
ライバル事務所が地方の数だけあること・**人気0なら comp 100 でも入らないこと**・
子ごとの戦績があればそちらを優先すること・`st.wins` が無いセーブで壊れないこと。
DOMに触らない関数だけを見る。

**数値を測る道具**（テストではない。数字を動かす前に見る）:

```
node tools/measure-jansou.js    直営店の三局面（客／一日の利益）
node tools/measure-office.js    遠征と日進行の釣り合い（§11 の A3 の四項目）
                                ＋ 一回の遠征の実り（A4.5 の再測。§5）
node tools/measure-fatigue.js   疲労が着順にどれだけ効くか（§9.2）。
                                --real で実対局を node で回す（一卓4秒）
```

**第五段の旗**（`--favor` / `--short` / `--away` / `--all`）を立てると、
`computeDay` の新しい入力が一本ずつ効く。立てなければ基準のまま。

どれも種を固定できるので、**前後の比較が「だいたい同じ」ではなく
完全一致で見られる。**測った顔ぶれも毎回出力する
（HANDOVER §4 の中盤が再現できなかったのは、それが残っていなかったため）。

**ブラウザで回すときは、セーブに `autoMatch: true` を入れること。**
`index.html` には `store.playRealMatch` があるので、夜のボトル勝負を
自動で「受ける」と**実対局に入ったまま誰も打たず、止まって見える。**
`tools/drive-jansou.js` が `jansou.html`（`playRealMatch` が無い）を
使っているのはこのため。実対局そのものの検証は手で押すこと。

## スカウトの中身（2026年9月・全3段 完了）

`docs/design/scout/spec.md`。遠征という器（日数・費用・同行者）は
A3 でできたが、中身は `drawOne` 一回の二値のままだった。ここを厚くする。

**三段すべて済んでいる。**到達点：

- 遠征中の朝に、**滞在先の店が静止した一枚**として出る。
  `JansouFloor` の描画をそのまま借りている。**一日を再生しない**
- 動くのは**乱数を使わない常時アニメ**だけ（打牌の手・ミラーボール）。
  `idle()` が時計を進めるだけで、タイムラインを持たない
- 店に**型が四つ**（古い雀荘／場末の店／街のガールズ雀荘／高級店）。
  型は行き先の県の `scale` から引く。大きい県ほど高級店
- 客をタップ＝**声をかける。一日3回まで。**観察はただ
- **雀ドルが一人もいない日がある**（規模の小さい県ほど多い）
- **雀ドルは必ず女性の見た目。ただの客は大半が男**（`FEMALE_RATE = 0.45`）。
  店の女性は平均4.6人で、**一日3回では総当たりできない**——だから癖を見る意味が出る
- **癖が出る**（A4.5-2）。打ち筋20種を癖6種へ束ねて、床に出す。
  **頭の上の印**（速度線・三点・凪の線）と**体まわりの物**（立直棒・晒し牌・長い河）の
  二系統で、**雀ドルは両方付き、ただの客はどちらか一つ**。
  声をかける前に見えているので、**ここで初めて「探す」が成立する**
- 声をかけて見つけたら、**発見の札に「あの癖はこの打ち筋だった」の一言**が付く。
  観察の報酬を言葉にするためだけのもので、判定には効かない
- **交渉に段階が入った**（A4.5-3）。口説く遠征が
  **一言 → 対局 → 交渉 → 足りなければ課題**の四拍になった。
  同行者に同郷の子がいれば場が和む。負けても好感度は積む
- **課題が事務所に届く**（`kind: 'quest'`）。空手で帰らせない。
  **相手ごとに一枠**で、二度失敗しても増えず上書きされる
- **好感度で契約金が下がる**（`favor/200`。満額100で半額）。
  緩むのは金だけで、格と大会実績の条件は動かない

### スカウトまわりの急所（ここを外すと崩れる）

- **`trip.shop` を `parlor` の下に置かないこと。**`Jansou.normalize()` は
  知らないキーを捨てる。置き場所は `trip` の下（最上位のセーブ）
- **擬似 `parlor` を `Jansou.normalize()` に通さないこと。**通すと
  `tables` が最低2に丸められるなど、自分の店の決めごとが混ざる。
  `JansouFloor` が読むキーだけを持たせる
- **種をそのまま渡さない。**線形合同法（`jansou.js` と同じ式）は
  隣り合う種の一手目がほとんど同じで、`seeded(day)` で引くと
  **毎日同じ型の店が出る**（実際に出た）。`ScoutShop.seeded` を通すこと
- **パレットの差し替えは浅いマージ。**`Object.assign({}, PAL, 上書き)`。
  ネオン・金・客・スタッフの色は共通のまま。
  差し替えるのは壁・床・卓と、**夜の灯り・消えたネオン**
  （その二つを足した理由は `scout/spec.md` §3.3）。
  **`PAL` 側の既定は直書きしてあった値のまま**なので、自分の店の絵は変わらない。
  `tools/test-scout.js` が錠を掛けている
- **`idle()` は `live.playing` を立てる。**客のタップ判定が
  `!live.playing` で弾くので、立てないと声をかけられない
- **床を作り直すときは前のを必ず `destroy()`。**`idle()` の rAF は
  自分では終わらない。`office.js` の `render()` が先頭で止めている
- **自動での発見はしない。**遠征中に毎日ただで一人見つかると、
  一日3回の上限も、どこを押すかの判断も意味を失う

### 癖まわりの急所（A4.5-2。ここを外すと「探す」が壊れる）

- **癖には系統が二つある。`beat`（頭の上の印）と `mark`（体まわりの物）。**
  雀ドルには**必ず片方ずつ**付ける（`quirksFor` がそうしている）。
  同じ系統を二つ持たせると片方しか描けず、**ただの客と見分けが付かない**
  ——「二つ付いている」が目で判るのは、この分けかたのおかげ
- **癖はスプライトの箱の外へ出す。1〜3ドットの印にしないこと。**
  最初は 12×16 の中に収めていたが、**店に20人座っていると見つけられなかった**
  （並べて比べれば違うが、探す状況ではまるで効かない）。
  離れて効くのは**シルエットが崩れているかどうか**。
  細い線も駄目——客の持ち物（`DECO` の 瓶・本・マイク）と同じ太さ・同じ列で、
  印として立たない。**2ドット以上の太さ＋暗い縁**（`outlined`）で浮かせる
- **動きに判別を担わせない。**離れていると振りかたは読めない。
  動くのは飾りで、効くのは形と色（`fast` シアン／`slow` 黄／`still` ピンク）。
  **動かしてよいのは明るさだけ。形を動かさないこと**——`slow` の三点を
  下から順に灯していたら、**瞬間によっては1点しか出ず**、小さな黄色い点
  ひとつになって見落とした。いまは三点とも常に描いて、色だけ変えている
- **灰は使えない。**`still` は最初 `closedTop`（灰）だったが、
  **場末の店の床が灰色**なのでそこで丸ごと消えた。ピンクにしたのは、
  シアン・黄と色相がいちばん離れていて、どの床の色とも当たらないため
- **型ごとに色を変えないこと。**個別には見やすくできるが、
  **店を移るたびに覚え直しになる。**癖は店ではなく人に付く。
  代わりに**全部の印を暗い縁で囲う**（`outlined`）——床は四型とも中間の
  明るさなので、明るい色を置くだけでは溶ける型が必ず出る
- **確かめかたは「全景から答えを伏せて探す」。**四型ぶんの店を用意して、
  「女性を探す → 印が二つ付いている子を探す」を踏む。
  **並べた比較は確認になっていない**（一度それで通してしまった）
- **絵で男女を出し分ける。**`grid(typeKey, frame, sex)` に `sex` を渡すと
  髪型が長い髪と短い髪に分かれる。**渡さなければいままでどおり**——
  自分の店の絵は1ドットも変わらない。`jansou.js` の `sex` は
  名前を作る一行だけで、床へ渡す `guests` には入れない
  （`tools/test-scout.js` が行ごと見て固定している）
- **写像は全単射にしない**（20 → 6）。一対一だと観察が対応表を引く作業になる。
  `tools/test-scout.js` が「癖ごとに打ち筋が2つ以上束なっている」を見ている
- **「癖がある＝雀ドル」にしない**（`spec.md` §4.3）。ただの客の一部にも付く
  （初期値25%）。ここを落とすと観察が完全情報になり、3回の上限が意味を失う
- **癖の先頭は打ち筋から来たほう。**発見の札の一言（`office.js` の `tellOf`）が
  `quirk[0]` を読む。二つめは系統を埋めるためのものなので、並びを入れ替えない
- **描くのは `live.clock` の位相だけから。乱数を混ぜない**（常時アニメと同じ扱い）。
  どの癖を配るかは `buildShop`（純関数）の中で使い切る
- **印の色は `PAL`（＝ PAL0）から取る。`pal` を使わない。**
  店の型で印の色まで変わると、型をまたいで癖を覚えられない
- **自分の店の床には出さない。**`jansou.js` は `guests` に `quirk` を入れないので、
  床は何も描かない。`tools/test-scout.js` が `jansou.js` を機械的に見て固定している

### 交渉まわりの急所（A4.5-3）

- **`Scout.evaluate()` と `RULES` の判定は変えていない。**
  変えたのは判定の前後に何を見せるかだけ（`scout/spec.md` §1）
- **条件の文面を二か所に書かない。**「事務所ランク4が必要です」は
  `RULES` の `detail` が正で、交渉の一行も課題の文面も**そこから引く**。
  `office.js` に書き写すと、片方を直したときに必ずずれる
  （`RULES.event` で一度通った話）。`tools/test-scout.js` が
  `office.js` の本文（コメントを外したもの）を機械的に見て固定している
- **セリフに条件を書かない。**`serifu.js` が持つのは性格の一言だけ。
  交渉の一行は「性格の一言」＋「`detail`」の合成（`Office.negotiate`）
- **同郷の一言は `scoutshop.js` に置く。**性格ではなく関係の話なので、
  19本も要らない（`serifu.js` に置くとキャラを足すたび二か所を埋めることになる）
- **課題の `when` は常に偽。**`Offers.fire()` は引かない。積むのは
  `Offers.push()` で、遠征の交渉が直接呼ぶ。発火の理由がプレイヤーの状態ではなく
  「その相手に会って勝った」という**出来事**だから
  （§8.1 の「日付を見ない」には触れていない）
- **課題は相手ごとに一枠**（`slot: 'quest:<id>'`）。`push` が同じ枠を置き換える。
  契約できたら `Office.dropQuest` が落とす
- **課題の定義に文面を焼き込まない。**`textOf` がそのつど `evaluate` を引く
  ——条件が変わっても札の文面が古びない
- **値引きは `costOf(chara, st)` に置く。`RULES` に触らない。**
  `st` を渡さなければ素の額（名鑑など、相手が決まっていない場面のため）

### テスト

`node tools/test-scout.js` … 927件。店の型の重み、パレットの浅いマージ、
雀ドルがゼロの日、声をかけなかった子が母集団に残ること、
朝に一度だけ引くこと、`calls` の上限、セーブの前方互換。
**癖**は、20種すべてが6種へ写ること・全単射でないこと・雀ドルは二拍で
系統が別なこと・ただの客は一拍か無しなこと・同じ種なら癖の配置も同じこと・
`jansou.js` が `quirk` も `sex`（床へ渡すぶん）も持たないこと。
**男女**は、絵が分かれること・`sex` 無しならいままでどおりなこと・
雀ドルが必ず女なこと・女の数が一日の上限を超えること。
**交渉**は、3場面が19種すべて埋まっていること・セリフに条件文が無いこと・
好感度0と100で契約金が2倍違うこと・格の条件は緩まないこと・
交渉の文面が `RULES` の `detail` そのものであること・
`office.js` が条件文を書き写していないこと・負けても好感度が積まれること・
同じ相手の課題が二件にならないこと・課題を果たせば `evaluate` が通ること・
契約したら課題が落ちること。

## 事務所の部屋（2026年9月4日・A6 第一段 完了）

`docs/design/office/room.md`。事務所の一室を **雀荘と同じ 200×164・同じ倍率**で描き、
物を押して各機能へ入る。到達点：

- 朝は部屋。壁に雀エイトの額・賞状・窓・ホワイトボード、床にパソコン・事務机・
  ソファ・観葉植物・扉。**押せる物には札**（雀エイト／ボード／メール／名簿／出かける）
- 押すと**シート**（部屋の上に下から上がる引き出し）が開く。中身はいままでの一覧の
  HTML そのまま。**部屋の下に一覧は続けない**
- **夜は同じ部屋が暗くなり、日報のシートが開いた状態で出る。**「明日へ」はその中と下の帯
- **遠征中の朝は枠に滞在先の店が入り**、下に「事務所に電話する」でシートを開く
- 380px で部屋と「今日を始める」がスクロール無しで入る（実測 492px）

### 部屋まわりの急所（ここを外すと崩れる）

- **`JansouFloor.mount()` を借りない。道具（`el` `rect` `gridRects` `fit` `drawCarpet`
  `drawFixtures`）だけ借りる**（`room.md` §3）。擬似 `parlor` を食わせると
  `normalize()` の丸めが混ざる（遠征先の店で通った道）
- **押せる物は全部 x 10〜190 の内側。部屋は横に送らせない。**380px の窓は 180 で、
  既定の送りが 10。`tools/test-office.js` が `layout()` を機械的に見て固定している
- **画素の層に文字を置かない。**札は当たり層（`.jnFlUi`）に `.jnFlTag` で出す。
  **札の有無＝押せるかどうか**なので、飾りに札を付けない
- **`renderMorning` 〜 `renderTrip` に `ofSecT` を戻さない。**テストの錠。
  一つでも一覧を続けたら、絵が一段増えただけの縦並びに戻る
- **シートは一度に一枚。**入れ子にしない。「受ける」で画面が替わるときは
  `sheet = null` にしてから `takeOffer`
- **夜の「明日へ」は二か所（シートの中と下の帯）。**自動化スクリプトは
  **シートの中の釦**を押すこと——帯の釦はシートに覆われていて、実クリックすると返らない
  （`tools/drive-office.js` の冒頭）
- **`roomCtl` は `render()` の先頭で必ず `destroy()`。**`idle()` の rAF は自分では終わらない
- **札は `placeHits()` で置き直す。**resize のたびに足すと二重に出る（実際に出た）
- **タイムカードを部屋に置かない。**「今日を始める」は下の帯だけ。
  部屋にもう一つ置くと「何の物か分からない箱」になった（実機で見た）

### テスト

`node tools/test-office.js` … `layout()` の当たり（x 10〜190・22以上・4離れている・
扉が雀荘の入口と同じ帯）、`roomView()`、`renderMorning` に `ofSecT` が無いこと、
既存の釦（`data-where` など）が残っていること、`JansouFloor` の export が値を変えていないこと。
`node tools/drive-office.js --days 30 --trip 3` … `index.html` を朝→営業→夜→朝で30日回す。
`--shots DIR` で朝・夜・遠征中の三枚を撮る。

## 雀エイト表（2026年9月・完了）

`docs/design/office/spec.md` §9.1。第五段のうち**数値と独立している部分**なので、
疲労・調子より先に入れた。到達点：

- 朝の画面に**全国上位八人**が出る。**実力と人気の二軸**（掛け合わせ）。
  表には別々の欄で出る。未契約の子も載る
- **始まりは八人の S級**（`RANK_INFO` の label が「雀エイト」なのと噛み合う）。
  育てて大会で勝つと、そこへ割り込んでいく
- 自分の子は金で縁取られる。入っていなければ
  「いちばん近いのは○○。八位まであと N 点」が出る
- **ライバル事務所の名前がここで初めて出る**（地方ごとに一つ、6事務所）

### 雀エイト表の急所

- **順位 ＝ 実力^0.6 × 人気^0.4**（`Office.FAME_MIX`）。**掛け合わせにすること。**
  足すと「強いが無名」「有名だが弱い」がどちらも入り、二軸である意味が消える。
  人気0なら実力100でも0になる。**アイドル活動がここに繋がる。**
  指数を 0.5:0.5 にすると **A級が二人入って S級八人が崩れる**（実測）
- **実力 ＝ `strengthOf` 6 : 実績 4**（`Office.POWER_MIX`）。
  `strengthOf` だけで並べると**A級が最初から S級を三人抜く**ので、
  「S級＝雀エイト」が初日から崩れる。実績を四割乗せると始まりが八人の S級になる。
  `tools/test-office.js` がこの前提ごと固定している
- **子ごとの大会戦績は `st.wins` に記録している**（A5 から始めた。
  `Tournament.recordResult` が大会の `finish()` で積む）。
  **記録があればそちらを優先し、無ければ事務所単位に落ちる**——
  A5 より前のセーブには何も無いため。`st.records` は事務所＝代表のもの
- **優勝だけを数える**（`best === '優勝'`）。出ただけでは載らない。上乗せの上限は40
- **元データを書き換えない。**`compFor` が写しを作る（`popOf` と同じ作法）
- **雀ドルごとに事務所を持たせない。**その子の地方の事務所に居ることにする
  （`Office.rivalOf`）。移籍そのものの振る舞いは `ROADMAP.md` [F]
- **同点は id で固定する。**毎朝並びが揺れると、壁の表として読めなくなる

## 疲労と調子（2026年9月4日・A5 完了）

`docs/design/office/spec.md` §9。**これで第五段が閉じ、事務所の五段階が全部済んだ。**

- **疲労** 0〜100。毎晩 −3／出勤1帯 +4／遠征1日 +11／依頼 +9／大会 +13／
  休みは追加で −12（合計 −15）。**夜だけ出勤は +1** でほぼ持続可能、
  フル出勤は11日で満タン。**回復しきる方向**（`ROADMAP.md` §1）
- **調子** −2〜+2。毎朝引く。疲労が高いほど悪い目に寄る
- どちらも `compEff = comp × (1 − 0.25 × 疲労/100) + 調子 × 2` に集まり、
  `paramsOf` の補間位置を戻す。**新しい計算系は作っていない**
- 所属一覧に**帯**（元気／やや疲れ／疲労／限界）と**きょうの増減**が出る。
  「休み」に切り替えると +5／日 が −15／日 に変わり、**抜け方がその場で見える**
- **推しファンの売上反映**と**出勤不足で開けられる卓が減る**も同時に入った

### 疲労まわりの急所（ここを外すと崩れる）

- **`settle` の中に疲労を置かない。**あそこは「スキップしても再生しても完全一致」が
  乗った純関数で、日を進めるのもあそこ一箇所。疲労は日が進んだあと（＝朝）に
  `Office.ensureFatigue` が追いかけて足す
- **疲労 → 調子の順で呼ぶ。**調子の重みは疲労の高さで変わる
- **`fatigueDay` / `condDay` を `shell.html` の `loadState` に書くこと。**
  落とすと**リロードのたびに引き直され、朝を開くたび調子が変わる**
  （`normalize()` の罠と同じ。実際に落としていた）
- **きのう何をしていたかは `assign` と `parlor.shifts` から読む**（`Office.actOf`）。
  **配置は変えるまで続く**ので別の記録を持たない。持つと古びる
- **一人ぶんは `assignFor`、全員ぶんは `assignOf`。**取り違えると
  配置が読めず、休みの子まで出勤扱いになる（実際にやった）
- **`skill` 側に別の経路を作らない。**`compEff` は `paramsOf` の `t` を通るので
  `skill` も一緒に動く。実測で効きのほとんどが `skill` 側から出ていた（§9.2）。
  もう一本足すと二重に効く
- **大会は枠を組むときだけ `tableCardOf` を通す。**`teamCards()` そのものは素のまま
  ——表示にも使われるのと、育成が `st.comp` を読み直すため
- **言葉は「ラスを引きやすくなる」で書く。**実測で目に見えるのは
  平均着順（+0.21）より**四着率（19.8% → 26.8%）と平均点（−1,900点）**のほう

## 次にやること

優先順。**ここがこのプロジェクトの「次の一手」の正。**
`docs/HANDOVER.md` §3 には、なぜこの順なのかの根拠だけを書いてある。

**直営雀荘は三つとも閉じた**（リニューアル5段階・卓の自由配置3段階・月末決算）。
**雀荘まわりに開いている宿題は無い。**

**`docs/design/office/spec.md` の五段階は全部閉じた。**
時間軸は一本になり、疲労・調子・雀エイト表まで入っている。

**いまの「次の一手」は [A6] 事務所を部屋にする**——`docs/design/office/room.md`。
**第一段（部屋と枠、シートに移す）は済んだ（2026年9月4日）。次は第二段（印）。**
段階は `room.md` §10（四段。第四段もやる）。**部屋の下に一覧を続けないこと**（§0・§12）。
その次は A7（遠征先で男性に声をかける。`ROADMAP.md` §6 に記録だけ）。

そのほかに開いている宿題は、下の 1 と、その先の一覧。

1. **A4.5 の再測を受けての調整**。`node tools/measure-office.js` の §5 が
   「一回の遠征で何人に声をかけ、何人見つかり、何人が契約に手が届くか」を、
   **押しかた二通り**（無作為／癖を読む）で出す。
   **測るところまでは済んでいて、まだ何も動かしていない。**
   `spec.md` §8 に残っているのは二つ
   （**ただの客に癖が付く割合25%**・**`CALLS_PER_DAY` の3**）。
   癖の周期は A4.5-2 で決めた
2. ~~**疲労・調子**~~ **→ 済んだ（A5・2026年9月4日）。**推しファンの売上反映と
   出勤不足の穴も同時に閉じた。新しい基準は `HANDOVER.md` §4
3. ~~**一度目の再測（A3）を受けての調整**~~ **→ 三つとも決着した。**
   二つは「動かさない」（遠征の費用・代表の夜の卓）、
   残る一つ（終盤は誰を出勤させても同じ）は A5 の `staffing` で閉じた

### 契約イベント（6人の解放）は済んだ

契約条件が `event` の6人（天城リオ No.001 を含む）は、第四段で解けた。
**図鑑は埋められる。**経路は

  好感度＋事務所ランク＋大会実績 → 事務所に話が届く → 受ける
  → その子の地方へ遠征して「口説く」→ 現地で勝つ → 契約

条件は `offers.js` の依頼テーブルにあり、**`RULES.event` は
「その依頼を受けたか」だけを見る**（`office/spec.md` §8.2）。
**両方に条件を書かないこと。**片方を直したときに必ずずれる。

そのあと（事務所の五段階が閉じてから）：

- **客タイプ21種の効果**。実装したのは荒らし・常連の主・推しファン（見た目のみ）の
  3つだけで、残りは `effect` を定義してあるが**どこからも読まれていない**
  （`jansou/spec.md` §6.2）。**配置コンボと同じく、`computeDay()` に触らない経路**
  （誰が座るか・臨時収入・常連の進み・buffs）で効かせられるものから順に入れる
- **推しファンの売上への反映**。`computeDay()` の入力に `favor` を足す形になる。
  `docs/HANDOVER.md` §4 の調整済み数値が動くので**再計測とセット**。
  事務所の第五段でアイドル活動が `pop` を上げるので、**そちらの再測に混ぜるのが安い**
- **字体まわり**（下の「既知の課題」）。拾い落とし（`×`）とフロアの
  `'MaruUI'` は直した。残るのは**元書体に無い3字**
  （`槓` `發` `簪`。差し替えるか代替を明示するかの判断が要る）だけ

置ける小物を増やす（コンボの種類を足す）のは、いまの6つが手に馴染んでから。

**雀荘に手を入れたら、純関数テストだけでなくブラウザでも回すこと。**
`node tools/drive-jansou.js --days 30` が30日を実際に押して通す。
`--old --days 12 --noskip` は割り込みの検査つき（上「テスト」）。
**事務所に手を入れたら `node tools/test-office.js` も通すこと。**

## 既知の課題

- ~~評判の高いセーブで営業が固まる~~
  **→ ゲームのバグではなかった（2026年9月3日）。**`base` でも同じ症状が出た。
  実体は、割り込みのポップアップに覆われたスキップ釦を自動化スクリプトが
  実クリックしようとして返らなくなっていたもの（**ドライバ側のデッドロック**）。
  CPU高止まりと `evaluate` の無応答が無限ループと同じ見え方をした。
  割り込み中はスキップ釦も消すようにして直した（`JansouFloor.skipHidden`）。
  経緯は `docs/design/jansou/monthly.md` §13。
  30日営業して出た月報のスクショも取得済み（`docs/design/jansou/month-report.png`）

- ~~狭い幅（2列配置）では、卓の上の2席に座った客が描かれない~~
  **→ 直った（2026年9月）。**卓の足元に席4つぶんを含めたので、
  置けた卓には必ず4席がある。狭い幅では横に送って見る。

- ~~字体の拾い落とし … `×` が別の書体で出ていた~~
  **→ 直った（2026年9月3日）。**`tools/make-font.py` の `source_chars()` が
  `ord(ch) > 0x2000` の字しか拾っておらず、**`×`（U+00D7）がサブセットから
  漏れていた**（`KANA_RANGES` は 0x007E までしか埋めないので、どこからも拾われない）。
  しきい値を `0x7E` にして回し直した。元書体には最初から入っていたので、
  **未収録ではなく拾い落とし。**
  症状が出ていたのは**雀荘の設備欄**（「客足 ×1.12」「回転 ×1.25」）。
  ブラウザに実際の書体を聞くと、直す前は
  `Rounded Mplus 1c Bold(19) + DejaVu Sans(1)` と**一文字だけ落ちており**、
  直したあとは `Rounded Mplus 1c Bold(20)` で代替書体が消えた。
  同じ規則で `§` `±` `Σ` も落ちていたが、こちらはコメントにしか出ない

- ~~フロアの書体指定 `'MaruUI'` は、どこにも定義されていない~~
  **→ 直った（2026年9月3日・コミット 8b27cb5）。**`src/jansou-floor.css` の
  3か所を `'Maru'` に直した。`@font-face` が定義している family は
  `'Maru'` だけ（`src/maru.css`）で、`'MaruUI'` は未定義の名前だったため、
  フロア・客カード・ボトル勝負が丸ごと `system-ui` に落ちていた

- **`槓` `發` `簪` の3字が丸ゴシックに無い。** 元書体（M PLUS Rounded 1c）自体に
  収録がないので、`tools/make-font.py` を回しても直らない。
  対局画面に出る字である（役名「槍槓」「四槓子」、役牌の「發」。
  `簪` は `characters.js` のキャラ説明）。いまはこの3字だけブラウザの
  代替書体で表示されている。直すなら、`maru.css` の `font-family` に
  丸ゴシック系のフォールバックを明示するか、`發` → `発`のように
  字を差し替えるかのどちらか。
